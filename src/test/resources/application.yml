info:
  app:
    name: EventosFull
    description: Projeto final da disciplina de Arquitetura de Software
    version: ${version}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL_TEST:jdbc:postgresql://localhost:5434/eventosfull-homolog}
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
